config {
  application {
    userId = 1
    zone = Europe/Paris
  }
  deezer {
    url = "http://localhost" // tests will inject WireMock port
    token = test_token
    maxResults = 100
    rateLimit = 10
  }
  github {
    token = "unused"
    repo = "user/repo"
    branch = "unused"
  }
}
